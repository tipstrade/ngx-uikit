import { HeightPlaceholderDirective } from "./height-placeholder.directive";

describe("HeightPlaceholderDirective", () => {
  it("should create an instance", () => {
    const directive = new HeightPlaceholderDirective();
    expect(directive).toBeTruthy();
  });
});

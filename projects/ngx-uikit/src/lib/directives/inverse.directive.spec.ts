import { InverseDirective } from "./inverse.directive";

describe("InverseDirective", () => {
  it("should create an instance", () => {
    const directive = new InverseDirective();
    expect(directive).toBeTruthy();
  });
});

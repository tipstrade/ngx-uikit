import { HeightViewportDirective } from "./height-viewport.directive";

describe("HeightViewportDirective", () => {
  it("should create an instance", () => {
    const directive = new HeightViewportDirective();
    expect(directive).toBeTruthy();
  });
});

import { ParralaxDirective } from "./parralax.directive";

describe("ParralaxDirective", () => {
  it("should create an instance", () => {
    const directive = new ParralaxDirective();
    expect(directive).toBeTruthy();
  });
});

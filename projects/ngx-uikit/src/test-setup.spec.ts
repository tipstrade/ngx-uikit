import UIkit from "uikit";
import Icons from "uikit/dist/js/uikit-icons";

// Use the Icons plugin before we start any tests
UIkit.use(Icons);

// eslint-disable-next-line @typescript-eslint/no-explicit-any
(globalThis as any).UIkit = UIkit;
